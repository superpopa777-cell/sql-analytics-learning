WHERE - фильрация данных

   Если SELECT отвечает на вопрос - что показать

   то WHERE отвечает на вопрос - какие строки оставить
   
❗Общий шаблон SQL с WHERE:
   
   SELECT columns(колонки)
   FROM table(из какой таблицы)
   WHERE condition(какие условия);

❗Пример:
Без WHERE(вернутся все пользователи):

   SELECT name, signup_date
   FROM users;

С WHERE(вернутся только пользоватлеи с именем Aice):

   SELECT name, signup_date
   FROM users
   WHERE name = 'Alice';

❗База операторов WHERE(равно/не равно, больше/меньше)

   Равно: WHERE name = 'Bob';

   Не равно: WHERE name != 'Bob'; или WHERE name <> 'Bob';

   Больше/меньше: WHERE signup_date > '2024-01-03';

❗Фильтрация по дате(частый кейс):

   SELECT *
   FROM users
   WHERE signup_date >= '2024-01-05';

Читается так: покажи пользователей зарегистрированных с 5 января и позже.

❗Несколько условий - AND(и)

   SELECT *
   FROM users
   WHERE signup_date >= '2024-01-01'
   AND signup_date <= '2024-01-07';
   
При этом условии, в диапазон попадут только те пользователи, которые лежат в этом промежутке.(оба эти условия должны быть истинны)

❗Несколько условий - OR и IN(или). При этом условии, В результат попадут все строки, для которых хотя бы одно условие истинно.(достаточно выполнения хотя бы одного из этих условий)

Вариант через OR:

   SELECT *
   FROM users
   WHERE name = 'Alice'
   OR name = 'Bob';

Вариант через IN(предпочтительней):

   SELECT *
   FROM users
   WHERE name IN ('Alice', 'Bob');

Итог: когда один столбец и несколько равных значений, то выигрывает IN.

❗Вариант через OR:

   SELECT *
   FROM users
   WHERE name = 'Alice'
      OR name = 'Bob'
      OR name = 'Charlie';

Вариант через IN(предпочтительней):

   SELECT *
   FROM users
   WHERE name IN ('Alice', 'Bob', 'Charlie');

Итог: когда один столбец и много равных значений, то опять выигрывает IN.

❗Если условия про разные стобцы(не как у нас было на примере выше, только про имена, а например имена и даты):

   SELECT *
   FROM users
   WHERE name = 'Alice'
      OR signup_date > '2024-01-05';

Итог: тут у нас задача выбрать пользователей с именем Alice или зарегестрированных после 5 января. В данном случае у нас условия для разных столбцов и разные операторы(если есть >, <, != → IN не подходит). В таких случаях применима только OR.
После теории были выполнены практические задания:
1)Задание 1 — базовая фильтрация
🎯 Задача

Напиши SQL-запрос, который:

выбирает столбцы:

name

signup_date

из таблицы users

оставляет только пользователей с именем Alice
2)🔹 ЗАДАНИЕ 2 — фильтрация по дате

Теперь следующий шаг.

🎯 Задача

Напиши SQL-запрос, который:

выбирает все столбцы

из таблицы users

оставляет пользователей, зарегистрированных ПОСЛЕ 2024-01-05

💡 Подсказка:

сравнение дат

оператор >
3)ЗАДАНИЕ 3 — несколько условий (AND)

Теперь усложняем, но логика уже знакома.

🎯 Задача

Напиши SQL-запрос, который:

выбирает все столбцы

из таблицы users

оставляет пользователей:

зарегистрированных не раньше 2024-01-01

и не позже 2024-01-07
4)🔹 ЗАДАНИЕ 4 — логика OR

Теперь проверим, как ты понимаешь «хотя бы одно условие».

🎯 Задача

Напиши SQL-запрос, который:

выбирает все столбцы

из таблицы users

оставляет пользователей:

с именем Alice

ИЛИ

с именем Bob
5)🔹 ЗАДАНИЕ 5 — переписываем через IN (хороший стиль)

Теперь сделаем то же самое, но аналитически лучше.

🎯 Задача

Перепиши предыдущий запрос через IN.

Условия те же:

таблица users

имена Alice и Bob
6)🔹 ФИНАЛЬНОЕ ЗАДАНИЕ 6 — проверка понимания логики

Это задание не сложнее предыдущих, но очень показательное.

🎯 Задача

Напиши SQL-запрос, который:

выбирает все столбцы

из таблицы users

оставляет пользователей:

с именем Alice

ИЛИ

зарегистрированных после 2024-01-05
7)Задание 7 — комбинируем AND + OR
🎯 Задача

Напиши SQL-запрос, который:

выбирает все столбцы

из таблицы users

оставляет пользователей:

с именем Alice

ИЛИ

с именем Bob

НО ТОЛЬКО если они зарегистрированы после 2024-01-01
8)🔹 ЗАДАНИЕ 8 — проверка понимания приоритета

Теперь задание специально на логику и приоритет, не на синтаксис.

🎯 Задача

Напиши SQL-запрос, который:

выбирает все столбцы

из таблицы users

оставляет пользователей:

с именем Alice

ИЛИ

с именем Bob, НО ТОЛЬКО если он зарегистрирован после 2024-01-05
9)🔹 Задание A
🎯 Задача

Напиши SQL-запрос, который:

выбирает все столбцы

из таблицы users

оставляет только пользователя с именем Bob
10)🎯 Задача

Напиши SQL-запрос, который:

выбирает все столбцы

из таблицы users

оставляет пользователей, зарегистрированных ПОСЛЕ 2024-01-01

❗❗❗ИТОГИ 1 этап 2 день:
1) Что WHERE фильтрует строки, а не столбцы
2) Как писать базовый SQL-запрос с условиями
3) Как фильтровать по значениям и датам
4) Чем отличается: AND (все условия) и OR (хотя бы одно)
5) Что IN — это: не «магия», а удобная форма OR для одного столбца
6) Где IN применять можно, а где нельзя
